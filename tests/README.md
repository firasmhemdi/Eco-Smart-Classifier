# Tests pytest

Ce dossier contient les tests automatises du projet Eco-Smart Classifier.

Commande recommandee :

```bash
pip install -r requirements-dev.txt
pytest
```

Objectif attendu :

- couverture >= 70% ;
- tests automatises reproductibles ;
- endpoints API verifies ;
- seuil minimal de performance verifie.

Etat actuel :

- 20 tests passent ;
- couverture `main.py` : 100% ;
- rapport HTML genere dans `htmlcov/`.
